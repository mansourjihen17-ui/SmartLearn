<!-- Navbar Start -->
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top p-0">
    <div class="container-fluid px-4">
        <a href="{{ path('app_homepage') }}" class="navbar-brand d-flex align-items-center py-3">
            <img src="{{ asset('images/logo.png') }}" alt="SmartLearn" height="42" class="me-2">
            <span class="fw-bold text-dark" style="font-size: 1.5rem; letter-spacing: .5px;">
        <i class="fa fa-book me-2 text-primary"></i>Welcome to SmartLearn Academy!
      </span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a href="{{ path('app_homepage') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;">Home</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_courses_index') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;">Courses</a>
                </li>

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% if is_granted("ROLE_INSTRUCTOR") %}
                        <li class="nav-item"><a href="{{ path('app_instructor_dashboard') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;">My Dashboard</a></li>
                    {% else %}
                        <li class="nav-item"><a href="{{ path('app_student_dashboard') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;">My Dashboard</a></li>
                    {% endif %}
                    <li class="nav-item"><a href="{{ path('app_profile') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;"><i class="fa fa-user-tie me-1"></i>{{ app.user.username }}</a></li>
                    <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;">Logout</a></li>
                {% else %}
                    <li class="nav-item"><a href="{{ path('app_login') }}" class="nav-link text-dark fw-bold px-3" style="font-size: 1.2rem;">Login</a></li>
                    <li class="nav-item ms-2">
                        <a href="{{ path('app_register') }}" class="btn btn-primary fw-bold px-3 py-2" style="font-size: 1.15rem;">Join Now</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
<!-- Navbar End -->